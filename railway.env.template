# Railway Environment Variables Template
# Copy these to your Railway service environment variables
# DO NOT create .env files with real values - use Railway's Variables tab

# Required: OpenRouter API Key
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Required: Server Configuration
NODE_ENV=production
PORT=8080

# Optional: Database Configuration (if using)
DATABASE_URL=your_database_url_here

# Optional: App Configuration
VITE_APP_URL=https://your-railway-domain.up.railway.app

# ==========================================
# SETUP INSTRUCTIONS:
# ==========================================
# 1. Go to your Railway project dashboard
# 2. Navigate to Variables tab
# 3. Add each variable above with your actual values
# 4. Replace 'your_openrouter_api_key_here' with your real OpenRouter API key
# 5. Update other values as needed
#
# NEVER commit .env files containing real API keys to git!
